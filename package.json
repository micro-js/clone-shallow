{
  "name": "@f/clone-shallow",
  "description": "Create a shallow clone of an object or array",
  "repository": "git://github.com/micro-js/clone-shallow.git",
  "version": "1.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/clone-obj": "^0.1.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
